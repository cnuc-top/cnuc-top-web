.el-aside {
  position: relative;
  height: auto;
  z-index: 1;
  overflow: visible;
  padding: 30px 0px;
}

.nb-aside {
  width: 300px;

  &.is-fixed {
    position: fixed;
    top: 15px;
  }

  &__title {
    font-size: 20px;
    margin-bottom: 24px;
    border-bottom: 1px solid $color-light-grey-ss;
    padding-bottom: 12px;
    color: #333;
  }

  .build {
    margin-bottom: 24px;
  }
}

.nb-main {
  // padding-left: 24px;
}
